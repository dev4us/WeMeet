type Meeting {
  id: Int!
  title: String!
  hashKey: String!
  thumbnail: String
  description: String!
  participants: [User]
  admin: User
  meetingDays: [Day]
  confirmDay: Day
  createdAt: String!
  updatedAt: String
}

type Query {
  Meeting: String!
}
