type CreateMeetingResponse {
  ok: Boolean!
  error: String
  Meeting: Meeting
}

type Mutation {
  CreateMeeting(
    title: String!
    description: String!
    thumbnail: String!
  ): CreateMeetingResponse!
}
